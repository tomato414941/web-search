# Crawler Server Environment Variables

# Indexer API URL (Full URL including path)
# Phase 1: Points to Frontend Server
# Get Frontend IP: ssh to Frontend Server, run `curl http://checkip.amazonaws.com`
INDEXER_API_URL=http://203.0.113.10/api/indexer/page

# API key for authenticating with Indexer
# MUST match the INDEXER_API_KEY from Frontend Server
# Get this by running on Frontend Server: cat ~/web-search/deployment/frontend/.env | grep INDEXER_API_KEY
INDEXER_API_KEY=change-this-to-match-frontend-api-key

# Crawler Configuration (optional, defaults shown)
CONCURRENCY=4
MAX_DEPTH=3

# How to get the required values:
# 1. Frontend IP: ssh to Frontend Server, run `curl http://checkip.amazonaws.com`
# 2. INDEXER_API_KEY: ssh to Frontend Server, run `cat ~/web-search/deployment/frontend/.env | grep INDEXER_API_KEY`
# 3. Update INDEXER_API_URL: Replace 203.0.113.10 with actual Frontend IP

# Future: When Indexer Service is separated from Frontend
# Just update INDEXER_API_URL to point to Indexer Service (e.g., http://indexer-ip/api/page)
