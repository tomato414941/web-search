# Frontend + Indexer Server Environment Variables

# Admin Authentication (Frontend)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-to-secure-password

# API Security
# API key for authenticating requests from Crawler to Indexer
# Recommended: generate with `openssl rand -base64 32`
INDEXER_API_KEY=change-this-to-secure-api-key

# Session Encryption Key (Frontend)
# Recommended: generate with `openssl rand -base64 32`
ADMIN_SESSION_SECRET=change-this-to-secure-session-secret

# Allowed Hosts (Frontend, comma-separated, set production domain)
ALLOWED_HOSTS=palebluesearch.com,localhost,127.0.0.1

# OpenAI API Key (Indexer - for generating embeddings)
# Required for semantic search functionality
OPENAI_API_KEY=sk-your-openai-api-key

# Crawler Service URL
# REQUIRED for Docker/Distributed setups (to control crawler from Admin Dashboard).
# - Docker Compose (same host): http://crawler:8000
# - Distributed (VM 2): http://<CRAWLER_IP>:8000
# - Local Dev (default): http://localhost:8000
CRAWLER_SERVICE_URL=http://<CRAWLER_IP>:8000

# Turso Database (production)
# Sign up at https://turso.tech and create a database
# turso db create web-search --location nrt
# turso db show web-search --url
# turso db tokens create web-search
TURSO_URL=libsql://web-search-xxx.turso.io
TURSO_TOKEN=eyJhbGciOiJS...

# Local SQLite (development, used when TURSO_URL is not set)
SEARCH_DB=/data/search.db

# How to generate secure values:
# openssl rand -base64 32
