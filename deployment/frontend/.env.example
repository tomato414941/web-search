# Frontend Server Environment Variables

# Admin Authentication
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-to-secure-password

# API Security
# API key for authenticating requests from Crawler
# Recommended: generate with `openssl rand -base64 32`
INDEXER_API_KEY=change-this-to-secure-api-key

# Session Encryption Key
# Recommended: generate with `openssl rand -base64 32`
SESSION_SECRET=change-this-to-secure-session-secret

# Allowed Hosts (comma-separated, set production domain)
ALLOWED_HOSTS=palebluesearch.com,localhost,127.0.0.1

# How to generate secure values:
# openssl rand -base64 32
