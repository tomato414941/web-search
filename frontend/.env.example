# Frontend Service Environment Variables
# Copy this to .env and customize for your environment

# ============================================================
# LOCAL DEVELOPMENT (Default)
# ============================================================
# Use these settings for local Python-based development
# CRAWLER_SERVICE_URL=http://localhost:8000
# DB_PATH=./data/search.db
# REDIS_URL=redis://localhost:6379/0

# ============================================================
# DOCKER COMPOSE (Service Names)
# ============================================================
# Use these settings when running with docker-compose
CRAWLER_SERVICE_URL=http://crawler:8000
DB_PATH=/data/search.db
REDIS_URL=redis://redis:6379/0

# ============================================================
# DISTRIBUTED DEPLOYMENT (External IPs)
# ============================================================
# Use these settings for distributed setup on separate servers
# CRAWLER_SERVICE_URL=http://13.159.200.66:8000
# DB_PATH=/data/search.db

# ============================================================
# Server Settings
# ============================================================
HOST=0.0.0.0
PORT=8080
WEB_CONCURRENCY=1

# ============================================================
# Security & Authentication
# ============================================================
SECRET_KEY=dev-secret-key-change-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme
INDEXER_API_KEY=dev-secret-key-change-in-production

# ============================================================
# External APIs
# ============================================================
# OpenAI API for embeddings (optional, fallback to None if not set)
# OPENAI_API_KEY=sk-proj-your-key-here

# ============================================================
# Host Validation
# ============================================================
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================================
# USAGE NOTES:
#
# 1. LOCAL DEVELOPMENT:
#    - Uncomment LOCAL DEVELOPMENT section
#    - Comment out DOCKER COMPOSE section
#    - Run: python -m uvicorn frontend.api.main:app
#
# 2. DOCKER COMPOSE:
#    - Use DOCKER COMPOSE section (default)
#    - Run: docker-compose up
#
# 3. PRODUCTION:
#    - Change all passwords/keys
#    - Set CRAWLER_SERVICE_URL to actual URL
#    - Add production domain to ALLOWED_HOSTS
#    - Set OPENAI_API_KEY if using embeddings
# ============================================================
