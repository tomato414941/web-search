# Crawler Service Environment Variables
# Copy this to .env and customize for your environment

# ============================================================
# LOCAL DEVELOPMENT (Default)
# ============================================================
# Use these settings for local Python-based development
# INDEXER_API_URL=http://localhost:8080/api/indexer/page
# REDIS_URL=redis://localhost:6379/0

# ============================================================
# DOCKER COMPOSE (Service Names)
# ============================================================
# Use these settings when running with docker-compose
INDEXER_API_URL=http://web:8080/api/indexer/page
REDIS_URL=redis://redis:6379/0

# ============================================================
# DISTRIBUTED DEPLOYMENT (External IPs)
# ============================================================
# Use these settings for distributed setup on separate servers
# Phase 1: Frontend統合
# INDEXER_API_URL=http://192.168.1.100/api/indexer/page
# Phase 2: Indexer分離後 (future)
# INDEXER_API_URL=http://192.168.1.200/api/page
# REDIS_URL=redis://localhost:6379/0  # Crawler's own Redis

# ============================================================
# API Keys (must match frontend)
# ============================================================
INDEXER_API_KEY=dev-secret-key-change-in-production

# ============================================================
# Crawler Tuning (Optional)
# ============================================================
# CONCURRENCY=4
# MAX_DEPTH=3
# CRAWL_TIMEOUT_SEC=30
# USER_AGENT=PaleBlueSearch-Crawler/2.0

# ============================================================
# USAGE NOTES:
#
# 1. LOCAL DEVELOPMENT:
#    - Uncomment LOCAL DEVELOPMENT section
#    - Comment out DOCKER COMPOSE section
#    - Ensure INDEXER_API_KEY matches frontend/.env
#    - Run: uvicorn app.main:app --reload
#
# 2. DOCKER COMPOSE:
#    - Use DOCKER COMPOSE section (default)
#    - Run: docker-compose up
#
# 3. PRODUCTION:
#    - Change INDEXER_API_KEY (must match frontend)
#    - Set INDEXER_API_URL to actual URL
#    - Tune CONCURRENCY/MAX_DEPTH for server capacity
#
# 4. FUTURE: Indexer Service Separation
#    - When Indexer is split from Frontend
#    - Just update INDEXER_API_URL to point to Indexer Service
#    - No code changes needed!
# ============================================================
